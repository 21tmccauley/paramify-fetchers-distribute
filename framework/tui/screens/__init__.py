"""Top-level pages of the TUI, each a self-contained module widget.

Modeled on the Bagels module recipe: a container that builds its skeleton in
compose() and (re)fills it with data in rebuild(), reading only from the App's
cached framework.api results.
"""
