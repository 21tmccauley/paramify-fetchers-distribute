# Runtime deps for fetchers + framework runner
python-dotenv
requests
pyyaml
jsonschema

# Web UI (framework/web) — optional unless you run the server
fastapi
uvicorn

# Terminal UI (framework/tui) — optional unless you run the console
textual>=1.0,<2.0
